
server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chiyanking/spring-cloud-config
          search-paths: /**
  application:
    name: config-server